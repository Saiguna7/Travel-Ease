{
    "buildCommand": "npm run vercel-build",
    "installCommand": "npm install --no-package-lock --platform=linux --arch=x64",
    "framework": "nextjs",
    "outputDirectory": ".next",
    "build": {
    "env": {
      "NPM_FLAGS": "--legacy-peer-deps"
    }
  }
  }